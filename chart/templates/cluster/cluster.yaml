apiVersion: cluster.x-k8s.io/v1beta1
kind: Cluster
metadata:
  name: {{ include "kamaji-cluster.clusterName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    cluster: {{ include "kamaji-cluster.clusterName" . }}
    {{- if .Values.cluster.cni }}
    cni: {{ .Values.cluster.cni }}
    {{- end }}
    {{- include "kamaji-cluster.labels" . | nindent 4 }}
spec:
  clusterNetwork:
    pods:
      cidrBlocks:
        {{- toYaml .Values.cluster.podCidrs | nindent 8 }}
    services:
      cidrBlocks:
        {{- toYaml .Values.cluster.serviceCidrs | nindent 8 }}
  controlPlaneRef:
    apiVersion: controlplane.cluster.x-k8s.io/v1alpha1
    kind: KamajiControlPlane
    name: {{ include "kamaji-cluster.clusterName" . }}-control-plane
  infrastructureRef:
    apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
    kind: HetznerCluster
    name: {{ include "kamaji-cluster.clusterName" . }}
